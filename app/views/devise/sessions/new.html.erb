<div class="col-lg-5 mx-auto">
  <div class="col-lg-12 mb-3">
    <h1 class="text-center logo">Log In</h1>
  </div>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-group">
      <%= f.label :email, "Email address" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "current-email", class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :password, "Password" %>
      <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
    </div>

    <p class="actions">
      <%= f.submit "Log In", class: "btn btn-block btn-primary"%>
    </p>

    <p>
      <%= link_to "Sign up", new_user_registration_path(resource_name), class: "btn btn-block mybtn btn-secondary" %>
    </p>
  <% end %>
</div>